.row
  .col-sm-12
    %h2 Post

    = simple_form_for(@post, html: {class: 'form-horizontal'}) do |f|
      = render 'form', f: f
      .form-group
        .col-sm-12
          = f.submit "Submit", class: 'btn btn-primary'
